{
  "name": "cinetop",
  "version": "1.0.0",
  "description": "Catálogo de filmes com CRUD usando JSON Server",
  "scripts": {
    "start": "json-server --watch db/db.json --port 3000 --static public"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
